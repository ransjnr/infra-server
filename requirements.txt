# Infra — full local / CI install from repo root.
# Install CPU PyTorch before the rest, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
# Then: pip install -r requirements.txt
#
# Per-service PYTHONPATH when running locally is documented in README.md.

fastapi>=0.115.0
uvicorn[standard]>=0.32.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
sqlalchemy>=2.0.0
httpx>=0.28.0
psycopg2-binary>=2.9.0
openai-whisper>=20240930
python-multipart>=0.0.9
transformers>=4.40.0
sentencepiece>=0.2.0
safetensors>=0.4.0
